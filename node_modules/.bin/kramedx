#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../kramedx/bin/kramed" "$@"
  ret=$?
else 
  node  "$basedir/../kramedx/bin/kramed" "$@"
  ret=$?
fi
exit $ret
